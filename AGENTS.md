# AGENTS.md

Repo conventions, stack, design system, and workflow live in
[CLAUDE.md](./CLAUDE.md) — read it before writing any code. Don't duplicate
its content here; update it there instead, so agents don't drift out of sync.

## Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/
before writing any code — Expo APIs move fast enough that general training
knowledge is likely stale.
