/**
 * The published Expo Snack URL for the "Run this site as an app" demo
 * section. The Demo section renders its placeholder state if this is ever
 * emptied out.
 */
export const snackUrl = "https://snack.expo.dev/@joshdhenry/joshhenry.info";

/**
 * Expo Snack's web-preview iframe host, used to detect this build running
 * as its own embedded Snack (confirmed via a running embed's iframe src).
 */
export const snackRuntimeHostname = "snack-runtime.eascdn.net";
